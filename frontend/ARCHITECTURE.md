# 🏛️ Gram Meter - Architecture Diagram

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                            │
│                     (Browser / Mobile)                           │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                      REACT APPLICATION                           │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  main.jsx    │──│   App.jsx    │──│  Dashboard   │         │
│  │  (Entry)     │  │  (Root)      │  │  (Page)      │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                  CONTEXTS                               │   │
│  │  ┌──────────────────────────────────────────────────┐  │   │
│  │  │  LanguageContext (Multilingual Management)       │  │   │
│  │  │  - translations (EN/HI/GU)                       │  │   │
│  │  │  - changeLanguage()                              │  │   │
│  │  │  - t() translation function                      │  │   │
│  │  └──────────────────────────────────────────────────┘  │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                  CUSTOM HOOKS                           │   │
│  │  ┌──────────────────────────────────────────────────┐  │   │
│  │  │  useMeterData()   - Real-time data + polling    │  │   │
│  │  │  useAlerts()      - Smart alerts management     │  │   │
│  │  │  useSimulateAlert() - WhatsApp simulation       │  │   │
│  │  │  useLivePower()   - Fluctuating power values    │  │   │
│  │  │  useNetworkStatus() - Online/Offline detection  │  │   │
│  │  └──────────────────────────────────────────────────┘  │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                  COMPONENTS                             │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │   │
│  │  │  Navbar      │  │  KPICard     │  │ UsageChart  │  │   │
│  │  │  - Language  │  │  - Live      │  │ - 24hr data │  │   │
│  │  │  - Network   │  │  - Cost      │  │ - Gradients │  │   │
│  │  │  - User      │  │  - Efficiency│  │ - Recharts  │  │   │
│  │  └──────────────┘  └──────────────┘  └─────────────┘  │   │
│  │                                                          │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │   │
│  │  │  AlertCard   │  │ EfficiencyBadge│ │ LoadingSpinner│ │   │
│  │  │  - Severity  │  │  - A-F Grade │  │ - Skeleton  │  │   │
│  │  │  - Actions   │  │  - Colors    │  │ - Spinner   │  │   │
│  │  └──────────────┘  └──────────────┘  └─────────────┘  │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                  │
└────────────────────┬───────────────────────┬────────────────────┘
                     │                       │
                     ▼                       ▼
┌─────────────────────────────┐  ┌─────────────────────────────┐
│      SERVICES LAYER          │  │     UTILITIES LAYER         │
│                              │  │                             │
│  ┌────────────────────────┐ │  │  ┌───────────────────────┐ │
│  │  API Service           │ │  │  │  Helper Functions     │ │
│  │  ─────────────         │ │  │  │  ────────────────     │ │
│  │  • getMeterData()      │ │  │  │  • formatCurrency()   │ │
│  │  • getAlerts()         │ │  │  │  • formatDateTime()   │ │
│  │  • simulateAlert()     │ │  │  │  • getTimeAgo()       │ │
│  │  • acknowledgeAlert()  │ │  │  │  • getEfficiencyGrade()│ │
│  │  • Retry logic         │ │  │  │  • detectAnomalies()  │ │
│  │  • Timeout handling    │ │  │  │  • calculateForecast()│ │
│  │  • Offline fallback    │ │  │  └───────────────────────┘ │
│  └────────────────────────┘ │  └─────────────────────────────┘
│                              │
│  ┌────────────────────────┐ │
│  │  Storage Service       │ │
│  │  ─────────────         │ │
│  │  • cacheMeterData()    │ │
│  │  • getCachedData()     │ │
│  │  • cacheAlerts()       │ │
│  │  • getLanguage()       │ │
│  │  • setLanguage()       │ │
│  │  • isCacheFresh()      │ │
│  └────────────────────────┘ │
└────────────────┬─────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BROWSER APIs                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │ localStorage│  │ fetch API   │  │ navigator   │            │
│  │ (Cache)     │  │ (Network)   │  │ (Online)    │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                    NETWORK LAYER                                 │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Online Mode:                                             │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  Django REST Framework Backend                     │  │  │
│  │  │  ────────────────────────────                      │  │  │
│  │  │  • GET  /api/meter-data/                          │  │  │
│  │  │  • GET  /api/alerts/                              │  │  │
│  │  │  • POST /api/simulate-alert/                      │  │  │
│  │  │  • GET  /api/efficiency-score/                    │  │  │
│  │  │  • POST /api/alerts/{id}/acknowledge/             │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Offline Mode:                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  Mock Data (Fallback)                              │  │  │
│  │  │  ────────────────────                              │  │  │
│  │  │  • MOCK_METER_DATA (constants/config.js)          │  │  │
│  │  │  • MOCK_ALERTS                                     │  │  │
│  │  │  • Cached data from localStorage                   │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 File Structure

```
gram-meter/
│
├── public/                       # Static assets
│
├── src/
│   ├── main.jsx                 # Application entry point
│   ├── App.jsx                  # Root component with providers
│   ├── index.css                # Global styles
│   │
│   ├── pages/
│   │   └── Dashboard.jsx        # Main dashboard page (270 lines)
│   │
│   ├── components/              # Reusable UI components
│   │   ├── index.js             # Component exports
│   │   ├── Navbar.jsx           # Navigation bar
│   │   ├── KPICard.jsx          # Metric display cards
│   │   ├── EfficiencyBadge.jsx  # A-F grade badges
│   │   ├── AlertCard.jsx        # Alert notifications
│   │   ├── UsageChart.jsx       # Usage analytics chart
│   │   └── LoadingSpinner.jsx   # Loading states
│   │
│   ├── contexts/                # React contexts
│   │   └── LanguageContext.jsx  # Multilingual management
│   │
│   ├── hooks/                   # Custom React hooks
│   │   ├── index.js             # Hook exports
│   │   └── useApi.js            # API & state management hooks
│   │
│   ├── services/                # Business logic services
│   │   ├── api.js               # API service with retry/timeout
│   │   └── storage.js           # localStorage management
│   │
│   ├── utils/                   # Utility functions
│   │   ├── index.js             # Utility exports
│   │   └── helpers.js           # Helper functions (20+ functions)
│   │
│   ├── constants/               # Configuration constants
│   │   ├── index.js             # Constant exports
│   │   ├── config.js            # App configuration
│   │   └── translations.js      # Language translations (3 languages)
│   │
│   └── assets/                  # Images, icons, etc.
│       └── plug.png             # Logo
│
├── .env.example                 # Environment variables template
├── package.json                 # Dependencies
├── vite.config.js              # Vite configuration
├── tailwind.config.js          # Tailwind configuration
├── eslint.config.js            # ESLint configuration
├── postcss.config.js           # PostCSS configuration
│
└── Documentation/
    ├── README_NEW.md            # Project overview
    ├── QUICK_START.md           # Quick start guide
    ├── IMPLEMENTATION_GUIDE.md  # Technical deep dive
    └── PROJECT_SUMMARY.md       # Completion summary
```

---

## 🔄 Data Flow

### 1. Real-Time Meter Data Flow

```
User Opens Dashboard
        │
        ▼
Dashboard.jsx mounts
        │
        ▼
useMeterData() hook triggered
        │
        ├─► Check localStorage cache
        │   └─► Show cached data (Optimistic UI)
        │
        ├─► Fetch from API
        │   │
        │   ├─► Success
        │   │   ├─► Update UI
        │   │   └─► Cache in localStorage
        │   │
        │   └─► Failure
        │       ├─► Use cached data
        │       └─► Show "Offline Mode"
        │
        └─► Start polling (every 3s)
            └─► Repeat fetch cycle
```

### 2. Alert System Flow

```
User Opens Dashboard
        │
        ▼
useAlerts() hook triggered
        │
        ├─► Fetch alerts from API
        │   │
        │   ├─► Success
        │   │   ├─► Filter by acknowledged status
        │   │   ├─► Cache in localStorage
        │   │   └─► Display in AlertCard components
        │   │
        │   └─► Failure
        │       └─► Use MOCK_ALERTS
        │
        └─► Poll every 10s for new alerts
```

### 3. WhatsApp Simulation Flow

```
User clicks "Simulate WhatsApp Alert"
        │
        ▼
useSimulateAlert() hook
        │
        ├─► Show loading state
        │
        ├─► Simulate API call (1s delay)
        │
        ├─► Generate result object
        │   {
        │     to: "+91 98765 43210",
        │     message: "Alert: ...",
        │     status: "sent"
        │   }
        │
        └─► Show success toast
            └─► Display sent details
```

### 4. Language Change Flow

```
User clicks language dropdown
        │
        ▼
Select Hindi/Gujarati/English
        │
        ▼
LanguageContext.changeLanguage()
        │
        ├─► Update context state
        │
        ├─► Save to localStorage
        │
        └─► Re-render all components
            └─► Use t() function for translation
```

---

## 🎯 Component Hierarchy

```
App.jsx
└── LanguageProvider
    └── Toaster (react-hot-toast)
        └── Suspense
            └── Dashboard.jsx
                ├── Navbar
                │   ├── Logo
                │   ├── Network Status
                │   ├── Language Dropdown
                │   └── User Avatar
                │
                ├── KPI Cards Row (Grid)
                │   ├── KPICard (Live Usage)
                │   │   ├── Icon (Zap)
                │   │   ├── Value + Unit
                │   │   ├── Trend
                │   │   └── Glow Effect
                │   │
                │   ├── KPICard (Daily Cost)
                │   │   ├── Icon (Home)
                │   │   ├── Value (₹)
                │   │   └── Subtitle
                │   │
                │   └── Efficiency Card
                │       ├── Score (94%)
                │       └── EfficiencyBadge (A+)
                │
                ├── Stats Row (Grid)
                │   ├── Today Usage
                │   ├── This Week
                │   ├── This Month
                │   └── Average Daily
                │
                ├── Charts & Alerts Row (Grid)
                │   ├── UsageChart (2 cols)
                │   │   └── AreaChart (Recharts)
                │   │       ├── Gradient
                │   │       ├── CartesianGrid
                │   │       ├── XAxis
                │   │       ├── YAxis
                │   │       ├── Tooltip
                │   │       └── Area
                │   │
                │   └── Alert Panel (1 col)
                │       ├── Header + Bell Icon
                │       ├── Alert List
                │       │   └── AlertCard (map)
                │       │       ├── Icon
                │       │       ├── Message
                │       │       ├── Details
                │       │       └── Ack Button
                │       │
                │       └── WhatsApp Button
                │
                ├── Bill Forecast Card
                │   ├── Icon (TrendingUp)
                │   ├── Forecast Message
                │   └── Stats (Projected/Comparison)
                │
                └── Offline Indicator (conditional)
                    └── Last Sync Time
```

---

## 🔐 Security Considerations

### Implemented:
✅ No hardcoded secrets
✅ Environment variables for API URL
✅ CORS-ready structure
✅ Input validation in forms (ready)
✅ Safe localStorage usage
✅ XSS protection (React default)

### Recommendations:
- [ ] Add authentication (JWT)
- [ ] Implement rate limiting
- [ ] Add CSRF tokens for POST requests
- [ ] Sanitize user inputs
- [ ] Use HTTPS in production

---

## ⚡ Performance Strategy

### Bundle Optimization:
```
Initial Load:
├── React 19 (core)
├── Tailwind CSS (purged)
├── Recharts (code-split ready)
└── App code (~450KB)

Lazy Loading:
└── Heavy components (charts, modals)
```

### Network Optimization:
```
API Calls:
├── Polling: 3s (meter) / 10s (alerts)
├── Retry: 3 attempts with 1s delay
├── Timeout: 10s per request
└── Cache: 5 minutes TTL
```

### Rendering Optimization:
```
React Optimization:
├── Functional components (faster)
├── Context API (minimal re-renders)
├── Suspense boundaries (graceful loading)
└── Memo-ready (can add useMemo/useCallback)
```

---

**Built with ⚡ for the Next Billion Users**
